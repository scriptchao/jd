.top
    .main
        .list
            &:hover
                span
                    display inline-block
                    animation rotate 0.2s forwards/*所有动画都是需要块级元素的*/

@keyframes rotate
    from
        transform rotate(0deg)
    to
        transform rotate(180deg)


